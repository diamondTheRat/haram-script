adopt rat named globals; # create a variable named 'globals'
grab globals; # select that variable
give {
    "my_variable": 2
}; # give it a dictionary

grab exec; # select python built-in 'exec' function
rat("print(f'the variable is {my_variable}')", swap); # run that function with the 'globals' variable as the globals for the function